[defaults]
inventory = hosts

[jenkins]
# Replace with the actual public IP of the EC2 instance after Terraform applies
jenkins_server ansible_host=<JENKINS_PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/your-key-name.pem

[all:vars]
ansible_python_interpreter=/usr/bin/python3